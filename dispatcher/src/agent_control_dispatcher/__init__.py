"""Task dispatcher: one agent session per item, one turn each, transcripts an operator reads.

Nothing here enforces a budget and nothing writes back; both are refusals on the server's turn path.
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.1.0"
